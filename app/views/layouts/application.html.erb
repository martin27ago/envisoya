<!DOCTYPE html>
<html>
<head>
  <title>EnviosYa</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <link rel="stylesheet" type="text/css" href="freelancer.css">
  <%= csrf_meta_tags %>
</head>
<body>
<% if current_user %>
<nav class="iconav" >
  <div class="iconav-slider">
  <ul class="nav nav-pills iconav-nav">
    <li>
      <a href="" class="btn-primary">  Ver pedidos  </a>
    </li>
    <li>
      <a href="" class="btn-primary">  Crear pedido  </a>
    </li>
    <li>
      <a href="" class="btn-primary">Editar datos</a>
    </li>
    <li align="right">
      Signed in as <strong><%= current_user.name %></strong>!
      <% if current_user.image_file_name.nil? %>
          <%= image_tag(current_user.imageFacebook, :alt => "rss feed") %>
      <%else%>
          <%= image_tag(current_user.image.url(:thumb), :alt => "rss feed") %>
      <%end%>
      <%= link_to "Sign out", signout_path, id: "sign_out" %>
    </li>
  </ul>
  </div>
</nav>

<% end %>
</body>
<div class="container col-md-5 col-md-offset-3">
<%= yield %>
</div>
</html>